# 多渠道本地化价格管理工具：AI 接手规则

## 权威入口

- GitHub：`https://github.com/NINI-YUL/price-manager.git`
- 默认分支：`main`
- 原机器正式目录：`E:\项目管理\渠道价格管理工具`；换电脑后以实际克隆目录为准。
- 代码、测试和 `docs/00_项目管理/01_项目状态.md` 优先于聊天记忆；发现不一致时先报告，不自行选择一方覆盖另一方。

## 接手时必须先读

1. `docs/00_项目管理/04_项目交接总览.md`
2. `docs/00_项目管理/01_项目状态.md`
3. `docs/01_产品设计/01_PRD.md` 与 `02_业务规则说明.md`
4. `docs/03_开发设计/06_数据字典.md`
5. 当前任务的执行说明
6. `docs/04_测试验收/01_测试验收标准.md`
7. `docs/05_AI协作/02_AI开发协作规则.md`

## 当前基线

- P1-001～P1-004 已完成。
- 下一任务是 P1-005 iOS 价格导入；尚未确认输入契约和执行说明，必须等待负责人明确确认后才能设计或实现。
- 当前自动化基线为 43 项测试通过，Ruff 通过，Qt 冒烟通过。
- P1-003 数据为 173 个国家/地区和 14 个 USD 档位；程序启动时幂等初始化。

## 工作边界

- 一次只推进一个已确认任务，不自动进入下一任务。
- 已完成的 P1-004 只解析并产生预览记录和问题列表，不写入正式 `channel_prices` 或 `price_versions`；后续任务不得绕过确认流程写正式价格。
- 不提前开发 Phase2/Phase3、汇率、调价、渠道 API、华为、小米或 RuStore。
- 金额使用 `Decimal`；不得用二进制浮点数做价格业务计算。
- 不修改真实 Excel 原文件；不得把真实价格文件、账号、密钥或未脱敏数据提交 Git。
- Google 原样本不在仓库中。需要回归真实结构时，要求负责人重新提供文件或确认本机路径。

## 实施与同步规则

- 修改前先说明目标、范围、影响文件、验证方式和待确认项；获得负责人确认后实施。
- 保留用户已有变更，不清理或覆盖无关文件。
- 每项完成必须执行对应测试、Ruff 和必要的启动冒烟，并报告实际结果。
- 完成后更新项目状态、更新记录和当前执行说明。
- 正式同步前检查 `git status` 与 diff，只提交本任务文件。
- 当前 Git 流程使用 GitHub Desktop 已有凭据或普通 `git`；不要要求安装 `gh`。
- 经负责人确认的任务完成后，同步本地正式仓库、最新版 ZIP 和 GitHub `main`，随后停止并等待下一任务确认。
