# 多渠道本地化价格管理工具：AI 接手规则

## 权威入口

- GitHub：`https://github.com/NINI-YUL/price-manager.git`
- 默认分支：`main`
- 原机器正式目录：`E:\项目管理\渠道价格管理工具`；换电脑后以实际克隆目录为准。
- 代码、测试和 `docs/00_项目管理/01_项目状态.md` 优先于聊天记忆；发现不一致时先报告，不自行选择一方覆盖另一方。

## 接手时必须先读

1. `docs/00_项目管理/04_项目交接总览.md`
2. `docs/00_项目管理/01_项目状态.md`
3. `docs/01_产品设计/01_PRD.md` 与 `02_业务规则说明.md`
4. `docs/03_开发设计/06_数据字典.md`
5. 当前任务的执行说明
6. `docs/04_测试验收/01_测试验收标准.md`
7. `docs/05_AI协作/02_AI开发协作规则.md`

## 当前基线

- P1-001～P1-009 及 Phase1 客户端验收整改已完成；整改执行基线为 `docs/03_开发设计/21_Phase1客户端验收整改执行说明.md`。
- Phase1 与 Phase2 均已正式通过客户端验收；Phase2 唯一执行基线为 `docs/03_开发设计/22_Phase2汇率分析执行说明.md`。
- 当前自动化基线为 157 项测试、Ruff 与正式/独立验收客户端 Qt 冒烟通过；schema v5 在七张 Phase1 表之外新增两张独立汇率表。
- 基础数据为 Google 与 iOS 国家/地区并集 191 个和 14 个 USD 档位；程序启动时幂等初始化。

## 工作边界

- 一次只推进一个已确认任务，不自动进入下一任务。
- P1-007 已实现受门禁保护的正式确认；任何后续入口仍不得绕过确认服务直接写 `channel_prices` 或 `price_versions`。
- 当前没有已授权开发任务；Phase3、价格导出、调价、渠道 API、华为、小米和 RuStore 需另行确认。
- 金额使用 `Decimal`；不得用二进制浮点数做价格业务计算。
- 不修改真实 Excel 原文件；不得把真实价格文件、账号、密钥或未脱敏数据提交 Git。
- Google、iOS 与 Web 原样本不在仓库中。需要回归真实结构时，要求负责人重新提供文件或确认本机路径。

## 实施与同步规则

- 修改前先说明目标、范围、影响文件、验证方式和待确认项；获得负责人确认后实施。
- 保留用户已有变更，不清理或覆盖无关文件。
- 每项完成必须执行对应测试、Ruff 和必要的启动冒烟，并报告实际结果。
- 完成后更新项目状态、更新记录和当前执行说明。
- 正式同步前检查 `git status` 与 diff，只提交本任务文件。
- 当前 Git 流程使用 GitHub Desktop 已有凭据或普通 `git`；不要要求安装 `gh`。
- 经负责人确认的任务完成后，同步本地正式仓库、最新版 ZIP 和 GitHub `main`，随后停止并等待下一任务确认。
- P1-007 唯一执行基线为 `docs/03_开发设计/18_P1-007导入检查与确认执行说明.md`；不得绕过 ERROR/警告/覆盖缩减门禁或把预览与旧版本合并。
- P1-008 已实现只读价格库、两种视角、独立版本组合、按年月分组/搜索历史版本、缺价语义和确认后刷新；不得在后续任务中绕过只读边界或提前扩展 Phase2/Phase3。
- P1-009 已实现 schema v4 状态审计、历史版本原子启用、归档校验和价格库联动；不得删除版本、单独归档当前活动版本或绕过版本管理服务直接改状态。

- Phase1 客户端验收整改已固定左侧导航、解析详情/解析问题 70:30 布局、国家码+中文名、中文调价方式/问题说明和统一金额显示；不得退回逐单元格重建筛选或改变 P1-007 门禁。
- Phase2 唯一执行基线为 `docs/03_开发设计/22_Phase2汇率分析执行说明.md`；七张 Phase1 表保持只读，只允许写独立汇率快照/获取日志，分析结果动态计算，不扩展 Phase3。

- Web 正式验收样本固定为 `D:\ChromeDownload\标准价格配置表 (1).xlsx`，SHA-256 `28c70d60126e4df2facd2759af271a7006c1a9d47e60f071c9fc3c7155fd9a38`，解析基线 420/30/20/14 且零问题；419 条备份不是验收输入。
