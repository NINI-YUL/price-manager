# P1-003 基础数据初始化执行说明

状态：已完成｜完成日期：2026-08-16

## 目标与边界

以可审计、幂等方式初始化国家/地区和 14 个 USD 档位。不得凭记忆补全国家数据；正式种子文件必须注明来源和提取日期。渠道别名作为独立配置，不扩充国家表结构。

## 建议文件

```text
src/database/seeds/countries.csv
src/database/seeds/price_tiers.csv
src/database/seeds/source_manifest.json
src/database/seeds/ios_country_aliases.csv
src/database/seed.py
tests/integration/database/test_seed.py
```

CSV 使用 UTF-8，纳入版本控制。国家字段为 `country_code,name_cn,name_en,default_currency`；档位字段为 `usd_price`。

## 14 档基线

2026-08-16 已由负责人确认，并与 Google 样本的 14 个商品列一致：

```text
0.99, 1.99, 4.99, 5.99, 9.99, 10.99, 14.99,
15.99, 19.99, 24.99, 29.99, 49.99, 69.99, 99.99
```

程序必须校验恰好 14 个唯一正数档位，并使用 Decimal 精确匹配。

## 国家基础范围

- P1-003 最初以 Google 样本中的 173 个唯一国家代码为范围；P1-005 按已确认规则扩展为 Google 与 iOS 国家/地区并集 191 个。
- Google 已有国家的 `default_currency` 保持 Google 样本值；iOS 新增 18 个国家/地区使用已确认的基础默认币种。iOS 实际价格记录仍以每行币种为准。
- 样本的 `国家/地区` 列全部为常量 `17`，不作为国家名称或国家标识。
- `name_cn`、`name_en` 使用固定版本 Unicode CLDR 48.2.0 的 `zh`、`en` 地区显示名，来源、输入/输出哈希和提取日期记录在附件资料及 `source_manifest.json` 中。
- 样本原文件不纳入 Git；测试只使用最小脱敏 fixture。

## 执行步骤

1. 根据已确认档位和 Google/iOS 并集 191 个国家代码准备 CSV，记录样本与 CLDR 名称来源。
2. 加载时验证编码格式、非空、唯一性、币种格式和 Decimal 精度。
3. 在单一事务内按自然键 upsert；重复执行结果一致。
4. 已存在且与种子冲突时默认失败并报告差异，不静默覆盖。
5. 输出新增、未变、冲突和失败计数。

## 必测场景

- 空库初始化成功；重复两次行数与内容不变。
- 恰好 14 个档位且 `9.99` 可精确匹配。
- 重复国家、非法编码、非法币种、零/负档位、非数字档位被拒绝并整体回滚。
- 冲突数据有可读差异报告。

## 验收门禁

数据库结构测试与种子测试通过；档位必须与上述清单逐项一致；国家代码/币种必须与已确认 Google 样本一致；中英文名称来源可追溯后，P1-003 才能标记完成。

## 实施与验收结果

- 已生成 191 条 `countries.csv`、14 条 `price_tiers.csv` 和 6 条 iOS 国家名称别名；清单、来源与 SHA-256 见 `docs/06_附件资料/01_基础数据来源记录.md`。
- 所有 CSV 在接触数据库前完成表头、数量、格式、非空、唯一性和档位基线校验。
- 空库首次执行新增 191 个国家/地区和 14 个档位；第二次执行新增均为 0，内容保持不变。
- 既有国家字段与种子不一致时输出字段差异并回滚本次全部新增，不静默更新。
- P1-005 扩展后的全量 `pytest` 共 57 项通过，Ruff 检查通过；P1-003 原始验收的 Qt 冒烟保持通过。
- P1-003 无遗留阻断项；Google 价格解析、商品映射和正式价格写入仍属于 P1-004 及后续任务。
