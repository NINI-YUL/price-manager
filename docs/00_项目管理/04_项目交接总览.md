# 项目交接总览

版本：V0.3｜交接日期：2026-08-17｜下一任务：P1-008

## 一分钟结论

这是一个 Windows 本地桌面价格管理工具。Phase1 的项目骨架、SQLite 六表结构/schema v3、191 个国家/地区、14 个 USD 档位、三渠道解析，以及统一检查、正式确认事务和来源归档均已实现并验收；下一步是 P1-008 价格库页面，其规则已经负责人确认，等待明确开始编码。

新账号不需要依赖旧聊天即可继续。请以 GitHub `main`、本文件、项目状态和自动测试为权威基线，先核验再开发。

## 权威资产

|项目|位置/值|说明|
|---|---|---|
|GitHub|`https://github.com/NINI-YUL/price-manager.git`|跨账号、跨电脑的唯一代码真相源|
|默认分支|`main`|当前采用直接、小步提交|
|P1-008 执行基线|`docs/03_开发设计/19_P1-008价格库查询执行说明.md`|规则已确认；未明确开始前不编码|
|P1-003 完成提交|`d74fc45a98c8a5c960a4b5e28d0e70ee06183ee4`|国家/档位种子与幂等初始化|
|原机器目录|`E:\项目管理\渠道价格管理工具`|同一台电脑继续时使用|
|本地数据库|`data/price_manager.db`|Git 忽略，可通过种子重新生成|
|Google 原样本|`D:\Desktop\google-product\google价格_get_result.xlsx`|Git 忽略；换电脑或新会话需重新提供|
|Google 原样本 SHA-256|`2e848e3e9dc442bf70c66dd8d0c85e521490aebc5655185d68bb4b422305689a`|用于确认拿到的是同一文件|
|iOS 原样本目录|`D:\Desktop\ios价格0817`|Git 忽略；换电脑或新会话需重新提供|
|iOS 目录摘要 SHA-256|`b71a301ba36bd42a3ba7a52688321d9d96a7843a2f70594c8302008dc070e52b`|按相对路径与文件内容计算的规范摘要|
|Web 原样本|`D:\ChromeDownload\标准价格配置表 (1).xlsx`|Git 忽略；换电脑或新会话需重新提供|
|Web 原样本 SHA-256|`28c70d60126e4df2facd2759af271a7006c1a9d47e60f071c9fc3c7155fd9a38`|用于确认拿到的是同一文件|

若代码、文档、聊天或本地数据库互相冲突，按以下优先级处理：自动测试与代码事实 → 项目状态/决策记录 → 当前任务执行说明 → 旧聊天。不得静默猜测。

## 已完成内容

|任务|完成内容|验收|
|---|---|---|
|P1-001|Python/PySide6 项目骨架、配置、目录、入口、依赖、基础测试|应用可启动|
|P1-002|SQLite 连接、6 张业务表、约束、索引、Reference Repository|schema 幂等，约束与回滚测试通过|
|P1-003|Google/iOS 并集 191 个国家/地区、14 个档位、来源清单、预校验、幂等事务写入|首次新增 191+14，第二次新增 0；冲突整体回滚|
|P1-004|Google `.xlsx` 只读解析、标准预览、统计、任务状态与 G001～G102 校验|真实样本 173/61/14/2,422，零问题；不写正式价格表|
|P1-005|iOS CSV 目录只读解析、调价方式、任务状态、schema v2 与 I001～I008/I102 校验|真实样本 175/43/14/2,450，手动 364、自动 2,086，零问题；不写正式价格表|
|P1-006|Web `.xlsx` 两行表头宽表只读解析、任务状态与 W001～W008/W102 校验|真实样本 30/20/14/420，零问题；收入/颜色忽略，不写正式价格表|
|P1-007|schema v3、统一导入检查页、确认门禁、完整快照事务和来源归档|真实三渠道 5,292 条端到端确认；3 个活动版本/成功任务，临时数据已清理|

当前验收基线：`pytest` 87 项通过，Ruff 通过，Qt 无界面启动通过；P1-007 关键模块合计覆盖率 88%。本地正式数据库应为 schema v3、191 个国家/地区和 14 个档位；数据库文件不提交 Git，开发验收不向正式库写测试价格。

## 已完成：P1-004

执行说明：`docs/03_开发设计/15_P1-004Google价格导入执行说明.md`。

已把确认的 Google 宽表解析为标准价格预览和问题列表：

- 解析 `.xlsx`，不修改原文件。
- 以 `国家代码`、`货币类型` 为准，忽略样本中恒为 `17` 的 A 列。
- 保留完整商品标题作为 `product_id`，支持 `&legacy-base` 后缀。
- 从 `_iap_<tier>` 提取档位，使用 `Decimal`，并验证档位存在。
- 产生可定位到 Sheet/行/列的标准记录、统计和 G001～G102 问题。
- 制作最小脱敏 fixture，真实样本不得提交 Git。
- P1-004 本身不创建正式版本；正式确认和来源归档已由 P1-007 统一实现。

- 8 个脱敏 fixture 覆盖正常与异常路径，全量错误码均有自动测试。
- 真实文件只读回归为 173 个国家/地区、61 种币种、14 档、2,422 条价格，零问题。
- 兼容模板错误声明 `A1:A1` 维度、实际数据为 `A1:Q174` 的情况；源文件哈希未变化。

## 已完成：P1-005

执行说明：`docs/03_开发设计/16_P1-005iOS价格导入执行说明.md`。

- 14 个档位目录各包含两份固定名称 CSV；档位来自目录名。
- 行字段 `N/Y` 分别映射 `MANUAL/AUTOMATIC`，不从文件名推断；收入字段忽略。
- iOS 行币种为价格权威值，国家默认币种不覆盖它；6 条中文别名独立配置。
- 国家范围扩展为 Google 与 iOS 并集 191 个；schema v2 增加调价方式，P1-007 已进一步升级为 schema v3。
- 真实目录只读回归为 28 文件、2,450 条、175 国家/地区、43 币种、14 档，零问题且源摘要未变化。

## 已完成：P1-006

执行说明：`docs/03_开发设计/17_P1-006三方网页价格导入执行说明.md`。

- 唯一结构匹配工作表使用两行表头；A 列积分为 14 个 USD 档位，每个国家按 `价格/收入` 成对。
- 顶层 `国家或地区(币种)` 的币种为网页价格权威值；国家覆盖允许是 191 个基础国家/地区的子集。
- 只读取价格，收入列和全部颜色/样式均无业务意义并忽略；Web 的商品 ID 和调价方式为空。
- 真实文件只读回归为工作表 `输出表`、区域 `A1:BI16`、420 条价格、30 个国家/地区、20 种币种、14 档，零问题且源哈希未变化。
- 预览只更新导入任务状态，不创建正式价格和版本。

## 已完成：P1-007

执行说明：`docs/03_开发设计/18_P1-007导入检查与确认执行说明.md`。

- schema v3 关联导入任务和正式版本，兼容 v1/v2 原子、幂等迁移。
- 三渠道共用导入检查页，支持后台解析、摘要、问题/价格列表、筛选和确认按钮门禁。
- 确认服务执行 ERROR 阻断、警告/覆盖缩减二次确认、重复摘要阻断、完整快照和单事务活动版本切换。
- Google/Web 复制原文件，iOS 目录压缩 ZIP；确认前重验摘要，失败回滚数据库并清理本次归档。
- 真实临时回归共确认 5,292 条价格，三渠道各一个活动版本，源摘要未变化；正式库未写测试价格。

## 下一任务：P1-008

P1-008 为只读价格库页面，规则已于 2026-08-17 确认，执行说明为 `docs/03_开发设计/19_P1-008价格库查询执行说明.md`。当前尚未授权编码，负责人明确说“开始 P1-008”后方可实施。

已确认范围：

- 默认档位视角，支持国家视角；档位视角按所选版本国家并集展示，可切换全部 191 个国家，国家视角固定 14 档。
- Google、iOS、Web 独立默认活动版本，也可选择历史版本；活动版本置顶，历史版本按导入时间“年 → 月”倒序分组、折叠并可搜索。
- 支持渠道、币种、国家/档位筛选、重置、手动刷新和 P1-007 确认成功后的刷新。
- 无版本、部分缺价和无筛选结果有独立空状态；缺价显示 `—`，不跨版本补值。
- 表头和价格居中，币种紧跟价格；本地价格保持 Decimal 精度，不换算、不额外取整、不使用科学计数法或千位分隔符。
- 页面只读，不修改价格或版本状态；P1-008 不提供导出，版本管理仍属于 P1-009。

## 后续未完成路线

|任务|状态|主要内容|
|---|---|---|
|P1-004|已完成|Google Adapter 与校验|
|P1-005|已完成|iOS Adapter、调价方式与 schema v2|
|P1-006|已完成|网页支付 Adapter、任务预览与校验|
|P1-007|已完成|导入检查、用户确认、正式事务与归档|
|P1-008|下一任务，规则已确认、待明确开始|只读价格库、两种视角、独立分组版本选择与筛选|
|P1-009|待设计/开发|版本管理、历史详情与归档来源查看|
|Phase2|暂缓|汇率分析|
|Phase3|暂缓|调价任务与修改校验|

P1-008～P1-009 应结合前一任务和真实样本逐步确认，不要提前一次性补充大量 Markdown 阻塞开发。

## 关键规则

- 渠道范围：Google Play、iOS App Store、三方网页支付。
- 核心模型：渠道、国家编码、USD 档位、币种、本地价格、版本。
- 技术：Python 3.11+、PySide6、SQLite、openpyxl、pytest、Ruff。
- 正式价格必须经过解析、校验和用户确认后才写入。
- 国家代码采用两个大写字母；币种采用三个大写字母。
- 金额用 `Decimal`；数据库使用 NUMERIC。
- 基础数据显示名固定来自 Unicode CLDR 48.2.0，范围为 Google/iOS 并集；渠道价格以原始行币种为准。
- 一次只推进一个已确认任务；发现规则缺失时先提问。

完整决策见 `docs/00_项目管理/03_设计决策记录.md`，完整业务规则见 `docs/01_产品设计/02_业务规则说明.md`。

## 新环境恢复命令

同一台电脑可直接打开 `E:\项目管理\渠道价格管理工具`。换电脑时先用 GitHub Desktop 或普通 Git 克隆仓库，然后在 PowerShell 执行：

```powershell
py -3.11 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\python.exe -m ruff check .
.\.venv\Scripts\python.exe -m src.database.seed
$env:QT_QPA_PLATFORM = "offscreen"
.\.venv\Scripts\python.exe -m src.main --smoke-test
```

如果 pytest 对系统临时目录报“拒绝访问”，使用项目 `data/` 下的唯一临时目录：

```powershell
$handoffTemp = Join-Path $PWD ("data\pytest-handoff-" + [guid]::NewGuid().ToString("N"))
.\.venv\Scripts\python.exe -m pytest --basetemp $handoffTemp
```

## 首次接手必须报告

在修改任何文件前，新账号应报告：

1. 当前分支、HEAD、是否与 `origin/main` 一致、工作区是否干净。
2. 实际读取到的已完成任务与下一任务。
3. 测试、Ruff、数据库行数和启动冒烟结果。
4. Google/iOS/Web 原样本是否可访问，摘要是否一致。
5. P1-008 执行说明是否完整可读，且负责人是否已经明确授权开始编码；不得重新猜测或扩大已确认规则。
6. 文档与代码是否存在冲突；若有，停止修改并请求确认。

可直接复制的话术见 `docs/05_AI协作/05_新账号启动话术.md`。
