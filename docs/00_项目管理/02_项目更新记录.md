# 项目更新记录

## V0.8 Phase2 正式通过客户端验收｜2026-08-21

- 负责人完成独立客户端复验并明确确认“验收通过”，Phase2 汇率分析正式验收完成。
- 验收覆盖手动获取汇率后的响应性、宽窗口筛选紧凑布局、统一分析配置弹窗、取消/统一应用行为及 `USD 99.99` 完整显示。
- 当前自动化基线为全量 157 项测试、Ruff、正式/验收客户端 Qt smoke；Standards 与 Spec 最终复审均为 clear。
- Phase2 继续保持只读边界；未修改价格、活动版本或 P1-009 状态/审计，未扩展 Phase3、导出、调价或渠道修改。
- Phase2 验收通过不等于授权 Phase3，后续阶段须由负责人另行明确启动。

## V0.7 Phase2 分析配置布局整改｜2026-08-21

- 主页面将统一汇率快照与三渠道价格版本收拢为两行只读摘要和“调整分析配置”按钮，减少常驻信息密度。
- 点击按钮后在同一模态弹窗中选择一个汇率快照及三个渠道版本；取消不改变当前分析，应用时一次性更新全部选择并重新计算。
- 历史版本选择仍不启用版本、不修改 P1-009 状态或审计；分析继续只读且不反写价格库。
- USD 档位筛选及其下拉列表设置最小宽度，`USD 99.99` 可完整显示；保留既有“加载更早快照”条件显示规则。
- 新增弹窗草稿/取消/统一应用回归，当前全量 157 项测试和 Ruff 全仓检查通过，等待负责人客户端复验。

## V0.6 Phase2 首次客户端验收卡顿整改｜2026-08-21

- 首次客户端验收发现：在 5,292 行分析结果可见时点击“获取最新汇率”，API 已完成去重但窗口持续未响应。
- 调用栈确认主线程停在逐个创建 `QTableWidgetItem` 的 `_render()`；可见表格配合 `ResizeToContents` 导致大量重复布局计算。
- `NOT_MODIFIED` 和失败缓存回退不再重载未变化分析；只有真正创建新快照时刷新数据。
- 分析表改为惰性 `QAbstractTableModel + QSortFilterProxyModel`，六类筛选不再重建数万个单元格。
- 新增未变化快照零表格变更、5,348 行筛选性能和宽窗口紧凑布局回归；全量 156 项测试和原始 5,292 行 Qt 心跳复验通过。
- 宽窗口下筛选标签不再被拉伸，名称与下拉框保持紧凑排列，多余空间留在筛选区右侧。
- 当前待负责人客户端复验；未改变 Phase1 七表、汇率快照规则或 Phase2 只读边界。

## V0.5 Phase2 汇率分析开发与自动化验证完成｜2026-08-21

- 负责人确认 ExchangeRate-API、USD 基准方向、不可变快照、活动/历史价格版本、偏差公式与阈值，并明确授权开始 Phase2 开发。
- schema 升级到 v5，新增独立汇率快照与获取日志；触发器禁止成功快照更新/删除，失败日志按 90 天清理且不影响成功快照。
- 实现运行时自动调度、手动获取、重复快照去重、缓存回退、历史快照分页和真实请求/完成时间；应用关闭期间不获取。
- 新增只读汇率分析页，支持三个渠道独立版本选择、统一汇率快照、方向/等级筛选、过期/缺失/空状态和完整精度提示。
- TDD 覆盖超时、HTTP 503/429、非法响应、正负 10%/20%、24 小时/7 天、七张 Phase1 表全过程只读、数据库异常和线程关闭超时。
- Standards 与 Spec 双轴 code-review 已执行并完成整改；全量 152 项测试、Ruff 与 Qt smoke 通过。
- 当前状态为开发与自动化验证完成、待负责人客户端验收；未扩展 Phase3、导出或渠道修改。

## V0.4 Phase1 正式验收通过与 Web 样本恢复｜2026-08-19

- 负责人已完成 Phase1 独立客户端验收并确认正式通过，Phase1 无剩余阻断开发任务。
- 从独立验收归档恢复 Web 历史原件到 `D:\ChromeDownload\标准价格配置表 (1).xlsx`；恢复后 SHA-256 为 `28c70d60126e4df2facd2759af271a7006c1a9d47e60f071c9fc3c7155fd9a38`。
- 恢复文件经临时数据库重新解析为 420 条、30 个国家/地区、20 种币种、14 档、0 错误、0 警告；未向正式数据库写入价格。
- 恢复前 419 条文件已备份为 `标准价格配置表 (1)_419条备份_20260818_174409.xlsx`，其 SHA-256 为 `05e2c50370f1789e3bc1b5dd253320fb1fda2307a203ecefe59d4dcd89388907`，不作为验收基线。

## V0.4 Phase1 客户端验收整改｜2026-08-19

- 主导航由顶部改为左侧；导入检查改为左侧“解析详情”、右侧“解析问题（错误/警告）”的 70:30 可拖动布局。
- 解析详情恢复左侧连续行号，筛选后按当前结果从 1 重新编号；原文件“行”字段继续保留。
- 解析详情新增国家码与中文国家/地区名双字段；iOS 调价方式和三渠道问题说明使用中文，保留稳定代码及来源定位；无问题时显示“本次解析无异常”。
- 本地价格显示统一为整数不带小数、非整数两位小数；超过两位有效小数时保留原值并提示。
- 详情表改用模型/代理筛选，不再为每次档位切换重建数千个单元格；真实 Google/iOS 样本切档约 21～35ms。
- 新增 4 项客户端验收反馈测试，自动化基线提升到 115 项；归档根目录、数据库 schema、P1-007 门禁和 Phase2 范围均未改变。

# 项目更新记录

## V0.4 Phase1 独立客户端验收环境｜2026-08-18

- 新增可双击的 `启动Phase1验收环境.cmd`，打开完整 Phase1 Windows 客户端界面。
- 验收数据库、来源归档和导出固定写入 Git 忽略的 `acceptance_runtime/`，与正式运行目录完全隔离。
- 验收窗口标题明确显示“Phase1 独立验收环境”，新增客户端验收指南和 2 项路径/schema/种子隔离测试。
- 当前全量自动化基线提升为 111 项；正式客户端与验收客户端均执行 Qt 无界面启动检查。
## V0.4 P1-009 完成｜2026-08-18

- 数据库升级到 schema v4，新增 `version_status_events`，为既有版本补迁移基线，并在导入确认和手动启用事务中写状态审计。
- 新增版本管理 Repository/Service，支持版本统计、任务追溯、Google/Web SHA-256 校验、iOS ZIP 完整性检查、越界路径保护和归档目录打开。
- 新增版本管理页面、版本详情/状态记录、渠道/状态/年月/文本筛选和带强制勾选的历史版本启用确认。
- 历史版本启用在单一事务中将当前 ACTIVE 归档、目标版本激活并写两条事件；审计失败时完整回滚。
- 版本管理与价格库按渠道联动；不提供版本删除、单独归档当前活动版本或价格导出。
- 新增 11 项 P1-009 集成/UI 测试；全量 109 项测试、Ruff 和 Qt 无界面启动作为完成验收基线。


## V0.3 P1-008 完成｜2026-08-18

- 新增只读价格库 Repository、查询服务和领域模型；所有数据库连接启用 `query_only`，未修改 schema v3。
- 主窗口新增“价格导入/价格库”导航；默认档位视角，可切换国家视角，并在确认成功后只刷新对应渠道活动版本。
- Google、iOS、Web 使用独立版本选择器；活动版本置顶，历史版本依据 `import_time` 按年/月倒序分组、折叠，并支持版本号、来源和日期搜索。
- 实现渠道、币种、国家/档位搜索、全部 191 国、重置和手动刷新；缺价显示 `—`，iOS 显示手动/自动调价标识。
- 表头和价格居中，币种紧跟价格；Decimal 不换算、不额外取整、不使用科学计数法或千位分隔符；价格详情只读。
- 新增 11 项 P1-008 集成/UI 测试；全量 98 项测试、Ruff 和 Qt 无界面启动通过，P1-008 核心模块合计覆盖率 92%，正式数据库仅用于只读界面检查。
- P1-008 未加入编辑、版本状态管理、导出、汇率或调价功能；下一任务 P1-009 尚待规则确认。
## V0.3 P1-008 规则确认｜2026-08-17

- 确认 P1-008 为只读价格库页面，不编辑正式价格、不改变版本状态、不提供导出，也不扩展到汇率或调价。
- 确认默认档位视角和国家视角：档位视角按所选版本国家并集展示并可切换全部 191 个国家；国家视角固定展示 14 个档位。
- 确认 Google、iOS、Web 分别默认活动版本，并允许只读选择历史版本；活动版本置顶，历史版本按 `import_time` 的年/月倒序分组、折叠和搜索。
- 确认渠道、币种、国家/档位筛选、重置、刷新、部分缺价、无版本和无筛选结果状态；缺价显示 `—`，不得跨版本补值。
- 确认所有表头和价格单元格居中，币种紧跟价格显示；本地价格保持 Decimal 精度，不转换、不额外取整、不使用科学计数法或千位分隔符。
- 新增 `docs/03_开发设计/19_P1-008价格库查询执行说明.md`；当前仅固化规则，等待负责人明确说“开始 P1-008”后编码。

## V0.3 P1-007 完成｜2026-08-17

- 数据库升级为 schema v3，为导入任务增加版本关联和唯一索引，并增加渠道/摘要查询索引；v1/v2 迁移原子、幂等。
- 新增三渠道共用的价格导入与检查页面：正确选择文件/目录、后台解析、摘要、问题/价格表、国家/档位/问题筛选和确认按钮门禁。
- 新增确认服务，落实 ERROR 阻断、WARNING/覆盖缩减二次确认、完整快照、重复来源阻断、版本号递增、单活动版本和全事务回滚。
- Google/Web 原文件复制归档，iOS 目录打包 ZIP；确认前重验来源摘要，失败清理本次归档，孤立归档只报告不自动删除。
- 三渠道真实来源在临时环境完成端到端回归：2,422 + 2,450 + 420 = 5,292 条价格、3 个活动版本、3 个成功任务、14 个 Google 商品映射；源摘要均未变化，临时真实归档已删除。
- 全量 87 项测试、Ruff 与 Qt 无界面启动通过；P1-007 关键模块合计覆盖率 88%；正式本地数据库未写入测试价格。

## V0.3 P1-007 规则确认｜2026-08-17

- 确认 P1-007 实现三渠道共用的“选择来源 → 解析 → 检查 → 确认入库”页面和确认服务；价格库与版本管理页面仍属于 P1-008/P1-009。
- 确认 ERROR 阻断、WARNING 二次确认、检查页不改数据，以及新版本完整替换旧活动版本、不合并旧价格。
- 确认覆盖缩减强提醒、相同渠道与摘要阻止重复入库、版本号按渠道/日期递增和单事务切换活动版本。
- 确认原文件复制归档：Google/Web 保存原 `.xlsx`，iOS 目录打包 ZIP；归档失败不创建版本。
- 确认 schema v3 为 `import_tasks` 增加版本关联，不新增预览明细表；当前仅完成执行说明，尚未授权编码。

## V0.3 P1-006｜2026-08-17

- 完成三方网页 `.xlsx` 两行表头宽表的只读 Adapter、标准预览、任务生命周期和 W001～W008/W102 校验。
- 以 A 列积分识别 14 个 USD 档位，以 `国家或地区(币种)` 和 `价格/收入` 列对识别国家、权威币种与价格；收入列及全部单元格颜色/样式忽略。
- 新增 Web 独立国家别名 `捷克共和国→CZ`；允许网页国家范围为 191 个基础国家/地区的子集，未知或歧义名称阻断。
- 真实样本回归结果为 30 个国家/地区、20 种币种、14 档、420 条价格；错误/警告/重复均为 0，回归前后 SHA-256 不变。
- 全量 72 项测试、Ruff 与 Qt 无界面启动通过；Web Adapter 与服务合计覆盖率 91%；预览未写入 `channel_prices` 或 `price_versions`。

## V0.3 P1-005｜2026-08-17

- 完成 iOS 14 档目录/28 个 CSV 的只读 Adapter、标准预览、任务生命周期和 I001～I008/I102 校验。
- 以档位目录名识别 USD 档位，按 CSV 行读取国家/地区、币种、价格与调价字段；`N` 映射 `MANUAL`，`Y` 映射 `AUTOMATIC`，收入字段忽略。
- 将国家种子扩展为 Google 与 iOS 并集 191 个，新增 18 个国家/地区和 6 个 iOS 中文名称别名；显示名继续固定使用 Unicode CLDR 48.2.0。
- 数据库升级为 schema v2，为 `channel_prices` 增加可空 `adjustment_mode`，并实现 v1→v2 原子、幂等迁移。
- 真实目录回归结果为 28 个 CSV、2,450 条价格、175 个国家/地区、43 种币种、14 档；手动 364、自动 2,086，错误/警告/重复均为 0，回归前后目录摘要不变。
- 全量 57 项测试和 Ruff 通过；P1-005 关键模块覆盖率 92%；预览未写入 `channel_prices` 或 `price_versions`。

## V0.3 P1-004｜2026-08-17

- 完成 Google `.xlsx` 只读 Adapter、标准价格/问题/统计模型，以及 `import_tasks` 预览任务生命周期。
- 实现表头识别、商品映射优先级、`_iap_<tier>` 档位提取、Decimal 价格解析、来源定位、去重与 G001～G102 全部校验规则。
- 新增 8 个最小脱敏工作簿 fixture，覆盖合法文件、重复表头、多匹配 Sheet、坏商品、坏行、冲突/去重、缺档与公式无缓存。
- 兼容真实模板工作表维度元数据错误声明为 `A1:A1` 的情况；解析前安全重算只读维度，不修改源文件。
- 真实样本回归结果为 173 个国家/地区、61 种币种、14 档、2,422 条价格，错误/警告/重复均为 0；回归前后 SHA-256 不变。
- 全量 43 项测试、Ruff 与 Qt 无界面启动通过；预览未写入 `channel_prices` 或 `price_versions`。

## V0.3 交接补充｜2026-08-17

- 新增根目录 `AGENTS.md`、项目交接总览、新账号接续操作手册和五组可复制启动话术。
- 修正 AI 启动指令中的过期任务状态与国家名称来源，明确新账号先核验、后确认启动 P1-004。
- 确立 GitHub `main` 为跨账号权威来源；旧聊天共享或导出仅作为可选参考。

## V0.3｜2026-08-16

- 将全部项目文档统一整理为 Phase1 开发启动基线。
- 新增 `13_P1-002数据库建立执行说明.md`。
- 新增 `14_P1-003基础数据初始化执行说明.md`。
- 新增 `15_P1-004Google价格导入执行说明.md`。
- 明确数据库约束、幂等初始化、Google 导入校验与错误分级。
- 将 P1-005～P1-009 设为后续按真实样本迭代，不阻塞首次开发。
- 确认完整 14 个 USD 商品档位。
- 完成 Google 真实样本只读分析：173 个国家代码、61 种币种、14 档商品列。
- 修正 Google 商品标题规则，支持 `&legacy-base` 后缀。
- 启动并完成 P1-001 项目初始化。
- 完成 P1-002：SQLite 连接生命周期、6 张业务表、约束、索引和最小 Reference Repository。
- 完成 P1-003：生成 173 条国家/地区与默认币种、14 条 USD 档位种子数据及来源清单。
- 中英文名称固定使用 Unicode CLDR 48.2.0；Google 样本和两个 CLDR 输入均记录 SHA-256。
- 实现种子文件全量预校验、单事务写入、重复执行零新增和既有冲突回滚；程序启动时自动完成幂等初始化。
- 数据库与项目测试共 25 项通过，Ruff、两次种子执行与 Qt 启动测试通过。

## V0.2｜2026-08-16

- 增加项目状态、Phase1 详细设计、开发环境、代码规范、P1-001 和 AI 启动文档。

## V0.1｜2026-08-16

- 建立项目、产品、规划、开发、测试和 AI 协作文档体系。
