# 测试验收标准

## 测试层级

- 单元：金额、编码、表头、商品 ID、去重和错误规则。
- 集成：SQLite schema、约束、种子、事务、Repository。
- Adapter：脱敏 Excel/CSV 正常与异常样本。
- UI 冒烟：启动、选择文件、检查门禁、查询筛选。

## Phase1 验收

1. 三渠道真实脱敏样本均能解析为统一模型。
2. 未知国家、未知档位、坏价格和冲突记录不能进入正式版本。
3. 确认事务失败时无半版本；每渠道最多一个活动版本。
4. 档位视角与国家视角结果与数据库一致。
5. 原文件、摘要、版本、导入时间和记录数可追溯。

## 质量门禁

`pytest` 全部通过；关键解析/校验模块需覆盖正常、边界、异常；无真实敏感数据进入仓库；人工验收记录实际样本版本、预期/实际结果和遗留问题。

## P1-008 验收要求

- Repository/查询服务覆盖活动与历史版本、三渠道独立版本组合、档位/国家视角、排序、筛选和缺价，确保不发生跨版本串数。
- UI 覆盖两种视角、年月分组/折叠/搜索版本选择器、渠道/币种/搜索筛选、重置、刷新、只读详情和各类空状态。
- 验证所有表头、价格和缺价符号居中，币种紧跟价格；本地价格整数不带小数、非整数显示两位，超过两位有效小数保留原值并提示；不换算、不使用科学计数法或千位分隔符。
- 使用临时数据库建立三渠道活动/历史版本，不向正式数据库写测试价格；P1-008 全流程不得写正式价格或改变版本状态。
- 详细验收以 `docs/03_开发设计/19_P1-008价格库查询执行说明.md` 为准。

## P1-008 验收记录｜2026-08-18

- Repository/服务测试覆盖活动/历史版本、跨渠道版本阻断、国家并集、191 国、14 档、币种筛选、缺价和 Decimal 格式。
- UI 测试覆盖双视角、居中展示、独立版本选择、年/月分组与来源搜索、iOS 调价标识、只读详情、重置、导航和查询失败。
- 验证 P1-007 确认成功后仅刷新对应渠道，保留其他渠道选择；页面未提供编辑、版本状态或导出入口。
- 全量 98 项测试通过，Ruff 通过，Qt 无界面启动通过；P1-008 核心模块合计覆盖率 92%。
- 测试使用独立临时数据库；正式数据库仅执行只读 UI 检查，未写测试价格或改变版本状态。

## P1-009 验收记录｜2026-08-18

- schema v4 初始化、v1/v2 兼容、v3 基线事件、事件外键和查询索引通过自动化验证。
- Repository/服务覆盖版本统计、任务追溯、Google/Web SHA-256、iOS ZIP、缺失/越界归档、导入审计和手动启用。
- 手动启用在同一事务切换两个状态并记录事件；审计写入失败时两个状态均回滚。
- UI 覆盖统计、版本号/来源搜索、渠道/状态/年月筛选、详情、状态记录、归档操作、确认勾选和价格库单渠道联动。
- 页面无删除、单独归档当前活动版本和价格导出入口；归档异常只警告，不阻止数据库快照启用。
- P1-009 新增 11 项测试，独立客户端验收环境新增 2 项隔离测试；当前全量 111 项测试、Ruff、正式客户端与验收客户端 Qt 无界面启动均通过。
- 测试使用临时数据库和临时归档；正式数据库仅执行 schema 迁移和空状态 UI 冒烟，不写测试价格。

## P1-003 验收记录｜2026-08-16

- 173 个国家/地区与 14 个档位来源清单、输入/输出 SHA-256 完整。
- 空库初始化、重复初始化、精确档位、非法编码/币种/档位、重复国家、冲突回滚均有自动化测试。
- 全量 25 项测试通过；Ruff 通过；命令行首次/二次初始化和 Qt 启动冒烟通过。

## P1-004 验收记录｜2026-08-17

- 8 个脱敏 `.xlsx` fixture 覆盖正常、文件损坏/超限、重复表头、多匹配 Sheet、未知国家/币种、坏商品、空值/文本/非正价格、冲突、去重、缺档和公式无缓存。
- 自动化覆盖 G001～G008、G101、G102，并断言 Decimal 精度、Sheet/行/列定位、统计和任务状态。
- 真实样本只读回归得到 173 个国家/地区、61 种币种、14 档和 2,422 条标准价格，错误、警告、重复均为 0；解析前后文件 SHA-256 一致。
- 全量 43 项测试、Ruff 和 Qt 无界面启动通过；预览期间 `channel_prices`、`price_versions` 均保持 0 条。

## P1-005 验收记录｜2026-08-17

- 测试动态生成最小脱敏 iOS CSV 目录，覆盖正确解析、目录/文件/表头错误、未知国家/币种、坏价格、坏 N/Y、冲突、去重、缺档、文件损坏/超限、任务失败和 schema v1→v2 原子迁移。
- 自动化覆盖 I001～I008、I102，并断言 Decimal 精度、CSV/行/列定位、国家别名、调价方式、统计、任务状态和零正式写入。
- 真实目录只读回归得到 28 个 CSV、2,450 条价格、175 个国家/地区、43 种币种和 14 档；手动 364、自动 2,086，错误/警告/重复均为 0；回归前后目录摘要一致。
- 全量 57 项测试和 Ruff 通过；P1-005 关键模块覆盖率 92%；`channel_prices`、`price_versions` 均保持 0 条。

## P1-006 验收记录｜2026-08-17

- 测试动态生成最小脱敏 Web 工作簿，覆盖正确解析、收入与颜色忽略、文件类型/损坏/超限、工作表与两行表头错误、未知国家/币种、坏档位/价格、公式无缓存、冲突、去重、缺档、任务失败和别名配置校验。
- 自动化覆盖 W001～W008、W102，并断言 Decimal 精度、Sheet/行/列定位、表头权威币种、统计、任务状态、源文件不变和零正式写入。
- 真实样本只读回归得到 30 个国家/地区、20 种币种、14 档和 420 条标准价格，错误、警告、重复均为 0；黄色填充和收入列未参与业务解析，回归前后文件 SHA-256 一致。
- 全量 72 项测试、Ruff 和 Qt 无界面启动通过；Web Adapter 与服务合计覆盖率 91%；`channel_prices`、`price_versions` 均保持 0 条。

## P1-007 验收记录｜2026-08-17

- 自动化覆盖 schema v1/v2→v3 迁移、任务版本关联、版本号、单活动版本、三渠道正式确认、完整替换、ERROR/警告/覆盖缩减门禁、重复摘要、来源变化、Google 商品映射、归档和回滚。
- UI 测试覆盖三渠道文件/目录模式、摘要/问题/价格展示、国家/档位/问题筛选、后台 Web 解析、确认按钮门禁、确认成功后禁止重复操作和主窗口接入。
- 三渠道真实来源在临时环境生成 3 个活动版本和 5,292 条正式测试价格，任务全部关联成功，Google 14 个商品映射正确，文件/ZIP 归档可读，源摘要未变化。
- 临时真实数据库和归档已删除；正式本地数据库未写入测试价格。
- 全量 87 项测试、Ruff 和 Qt 无界面启动通过；P1-007 关键模块合计覆盖率 88%。

## 文档验收

任务完成后同步项目状态、更新记录、决策变化和对应执行说明。未确认的假设必须显式列出，不得伪装成已验收规则。

## Phase1 客户端验收整改记录｜2026-08-19

- UI/模型测试覆盖左侧导航、70:30 左详情右问题布局、国家码/中文名、iOS 中文调价方式、中文问题说明和无异常空状态。
- 金额测试覆盖整数无小数、一位/两位小数统一两位、超过两位有效小数保留原值与提示。
- 筛选测试确认国家/档位切换只更新代理模型，不重置来源模型；真实 Google 2,422 条和 iOS 2,450 条切档约 21～35ms。
- 当前自动化基线为 115 项；Ruff、全量测试、正式客户端和独立验收客户端冒烟必须在正式目录全部通过后才允许推送。
- 归档路径变更、完整主题视觉优化和 Phase2 不属于本次验收。

## Phase1 最终客户端验收记录｜2026-08-19

- 负责人已使用独立客户端完成 Phase1 验收并确认通过。
- 此前客户端反馈均已整改：左侧导航、详情/问题布局、中文字段/问题、金额格式、快速筛选，以及筛选结果按 `1～N` 重新编号。
- Web 权威样本恢复为 SHA-256 `28c70d60126e4df2facd2759af271a7006c1a9d47e60f071c9fc3c7155fd9a38`；重新解析为 420 条、30 国、20 币种、14 档且零问题。
- Phase1 无遗留阻断缺陷；完整视觉优化、归档路径变更与 Phase2 不属于本次结项范围。

## Phase2 自动化与客户端验收记录｜2026-08-21

- Adapter/服务测试覆盖 ExchangeRate-API 正常与异常响应、USD 基准校验、供应商更新时间、自动/手动获取、缓存命中、重复快照和失败恢复。
- schema v5 测试覆盖两张独立汇率表、不可变成功快照、供应商更新时间唯一性、永久保留成功记录和 90 天失败日志清理。
- 分析测试使用 Decimal，覆盖价格记录自身币种、理论价、正负偏差、10%/20% 边界、活动/历史价格版本和单一汇率快照绑定。
- UI 测试覆盖后台获取、紧凑配置摘要、弹窗草稿/取消/原子应用、完整 USD 档位、筛选、懒加载和无编辑/导出/状态修改入口。
- 七张 Phase1 表在 Phase2 分析与获取流程中保持不变；5,348 条分析结果的加载与筛选性能通过验收，历史数据不一次性进入内存。
- 全量 157 项测试、Ruff、正式客户端与独立验收客户端 Qt 冒烟通过；TDD 实施后的 Standards 与 Spec code-review 均无遗留阻断项。
- 客户端反馈的获取时窗口未响应、筛选布局、USD 档位截断和分析配置弹窗均已整改，负责人确认 Phase2 验收通过。详细证据以 `docs/03_开发设计/22_Phase2汇率分析执行说明.md` 为准。

## v0.1.1 补丁验收记录｜2026-08-25

- 回归测试覆盖价格导入国家/地区与档位筛选框在 1180×720 最小支持窗口下的正文宽度、实际控件宽度和弹出列表宽度。
- 较长的 `国家码｜国家/地区` 选项与档位选项均按当前字体和 Qt 样式动态计算，不依赖固定 DPI 像素值。
- 全量 158 项测试、Ruff、正式客户端与独立验收客户端 Qt 无界面启动均通过。
- 负责人已使用独立客户端和正式客户端完成复验并确认通过。
- 本补丁不修改数据库 schema、正式价格、版本状态、汇率数据或 Phase2 只读分析规则。
