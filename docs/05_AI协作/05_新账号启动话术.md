# 新账号启动话术

以下内容可直接复制到新 ChatGPT/Codex 账号。推荐先使用第一段，只读核验通过后再使用第二段。

## 话术一：首次接手，只读核验

```text
你正在接手“多渠道本地化价格管理工具”。仓库地址是 https://github.com/NINI-YUL/price-manager.git，正式分支为 main；同一台电脑上的正式目录是 E:\项目管理\渠道价格管理工具，换电脑时以实际克隆目录为准。

本轮只恢复上下文和核验基线，不修改项目源代码或文档、不提交、不推送，也不开始 P1-004。测试与种子幂等验证只能使用临时数据库或 Git 已忽略的运行目录。

请依次执行：
1. 阅读仓库根目录 AGENTS.md。
2. 阅读 docs/00_项目管理/04_项目交接总览.md、01_项目状态.md、03_设计决策记录.md。
3. 阅读 PRD、业务规则、数据字典、测试验收标准、AI 开发协作规则和 P1-004 执行说明。
4. 检查 git status、当前分支、HEAD、origin/main 和最近提交。
5. 运行现有测试、Ruff、种子初始化两次和 Qt smoke test；不要修改真实 Excel。
6. 报告：已完成任务、下一任务、代码/文档是否一致、测试结果、数据库国家/档位数量、外部样本是否可用、P1-004 开始前仍需我确认的事项。

已知基线：P1-001～P1-003 已完成；P1-003 完成提交为 d74fc45a98c8a5c960a4b5e28d0e70ee06183ee4；当前应有 25 项测试、173 个国家/地区、14 个 USD 档位。若实际结果不一致，请停止并列出证据，不要自行修复或覆盖。
```

## 话术二：确认开始 P1-004

发送此话术时，同时重新上传 `google价格_get_result.xlsx`，或提供新账号可访问的本机路径。

```text
基线核验通过，现确认开始 P1-004 Google 价格导入。

请严格依据 docs/03_开发设计/15_P1-004Google价格导入执行说明.md 实施。本任务只实现 Google Excel 的只读解析、标准价格预览、统计和问题列表，不创建正式价格版本，不写 channel_prices 或 price_versions，不开发 UI 检查确认流程，也不进入 P1-005。

真实文件 google价格_get_result.xlsx 只读使用，不修改、不移动、不提交 Git。先验证文件 SHA-256 是否为 2e848e3e9dc442bf70c66dd8d0c85e521490aebc5655185d68bb4b422305689a，并核对 Sheet1/A1:Q174 画像。测试仓库只允许保存最小脱敏 fixture。

开始修改前先向我报告：
- 计划新增/修改的文件；
- StandardPrice、问题对象和 Adapter 的最小设计；
- G001～G102 的实现与测试覆盖；
- 哪些内容明确不在本任务；
- 是否存在执行说明未覆盖、必须由我确认的业务选择。

没有新增业务选择时可按既定说明完成实现和验收。完成后运行全部 pytest、Ruff、真实样本只读回归和必要的启动冒烟，更新项目状态/更新记录/P1-004 执行说明；仅提交本任务文件，同步本地正式目录、最新版 ZIP 和 GitHub main，然后停止等待我确认下一任务。
```

## 话术三：开始后续任一任务

将 `{任务编号}`、`{任务名称}` 和 `{执行说明路径}` 替换后使用。

```text
现确认开始 {任务编号} {任务名称}。

先读取 AGENTS.md、项目交接总览、项目状态、业务规则、数据字典、测试标准和 {执行说明路径}，再检查代码与测试的真实状态。修改前报告任务范围、影响文件、测试、外部样本和待确认项；只实现该执行说明，不自动进入下一任务，不扩展 Phase 范围。

如文档、代码、样本或已确认规则冲突，先给出证据、影响和建议，等待我决定。任务完成并验收后，更新状态文档，只提交本任务文件，同步本地、最新版 ZIP 和 GitHub main，然后停止等待确认。
```

## 话术四：防止继续无限补文档

```text
请先区分“阻塞当前开发的必需确认”和“可以边开发边补充的文档”。除非缺失信息会改变数据模型、解析规则、用户流程或验收结果，否则不要新增前置 Markdown 阻塞开发。优先完成当前已确认任务的小步实现、测试和真实样本验证；需要新增文档时，说明它解决的具体风险。
```

## 话术五：阶段结束时生成下一份交接快照

```text
请在当前任务验收后生成交接快照：记录完成提交、变更文件、实际测试结果、数据/样本哈希、已确认规则、遗留问题、下一任务边界和启动话术；同步更新 AGENTS.md、项目交接总览、项目状态和更新记录。快照必须让一个看不到本次聊天的新账号，仅凭仓库即可继续。
```
